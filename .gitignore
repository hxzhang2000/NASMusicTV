# Gradle
.gradle/
build/
local.properties

# IDE
.idea/
*.iml
*.ipr
*.iws

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Android
*.apk
*.aab
*.ap_
*.aab_
*.keystore
*.jks

# Kotlin/Native
*.klib

# Kotlin daemon / compiler working directory
.kotlin/

# Environment
.env
.env.local

# ProGuard
proguard/
*.mapping

# Signing
keystore.properties
ci-keystore.jks

# CI release notes (generated by workflow)
release_notes.md

# OpenCode internal state
.omo/

# App release builds
app/release/

# Obsidian vault local config
.obsidian/

# CodeGraph code intelligence index
.codegraph/

# CI / debug leftovers
# ⛔ 约定（AGENTS.md → Repo-root hygiene）：日志/临时文件一律放 logs_temp/，不要落根目录。
#    下列条目只是「历史遗留 + 安全网」，不是允许在根目录建这类文件的许可。
build_log.txt
build_rel.log
build_rel2.log
release_build.log
build_*.log
build_*_result.txt
build_*_tail.txt
build_*_detail.txt
diag.txt
ci_logs.zip
logs.zip
jobs.json
jobs3.json
defex.txt
screen.png
screenshot.png

# 根目录临时杂项（2026-09-24 追加：一次性预览图 / 测试计数 / agent 产物 json）
build_test_counts.txt
orientation_icon_preview.png
.orientation_icon_preview.png.artifact.json
image48294fcc-0da3-4613-8282-68ab5cc6e499.png

# Dev tools / agent internal state
.workbuddy/
.workbuddy-ai/
.joycode/
.box-agent/

# WorkBuddy 设计文件（*.miora）与其导出素材
docs/*.miora
docs/*_assets/

# Local build/dev output dirs
logs_temp/
output/
# box-agent 会话产物目录（`<日期>-<会话ID前8位>`，自动生成在项目根，不入仓）
# 例：2026-09-13-384e30ce/ —— 由 agent 运行时创建，内含 output/tasks/… 截图等
20??-??-??-????????/
# 依赖 classpath 转储等临时产物（工具生成，不入仓）
.cowork-temp/
